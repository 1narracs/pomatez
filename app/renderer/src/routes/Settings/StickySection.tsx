import React from "react";
import { StyledSectionSticky, StyledStarButton } from "styles";

const StickySection: React.FC = () => {
  return <StyledSectionSticky>//-.-\\</StyledSectionSticky>;
};

export default StickySection;
